Dear <%= @scenario.name %>,

We hereby send you a direct link to the mix you created using the <%= t(@partition.name) %>.

<%= scenario_url(@scenario) %>

You can also use the link below to load your Energy Mix in the Energy Transition Model. This allows you to fine tune your scenario, using many more options. This is what policy makers use.

<%= @etm_scenario_url %>

Any ideas to improve the <%= t(@partition.name) %> or reactions can be sent in an e-mail to: mixer@quintel.com

Kind regards,

<%= t(@partition.name) %>
