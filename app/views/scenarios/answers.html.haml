.scenario_answers
  %h2= t 'scenario.answers'

  %dl
    - @scenario.answers.each do |a|
      %dt
        #{a.question.number}.
        = a.question.question
      %dd
        - if a.answer
          = a.answer.answer.html_safe
        - else
          = t 'scenario.no_answer'          
        - if APP_CONFIG["score"] && a.score
          = t 'scenario.score', :score => a.score
