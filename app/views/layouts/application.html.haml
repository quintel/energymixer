!!! 5
%html
  %head
    %title EnergyMixer
    = stylesheet_link_tag '960'
    = stylesheet_link_tag 'all'
    = javascript_include_tag :defaults
    = csrf_meta_tag
    = yield :head
  %body{:id => "#{controller.controller_name}_#{controller.action_name}", 
        :class => controller.controller_name}
    .container_12
      %header
        %nav
          = link_to 'Admin', questions_path
      - flash.each do |k,msg|
        .flash{:id => k}
          = msg
      #content
        = yield
